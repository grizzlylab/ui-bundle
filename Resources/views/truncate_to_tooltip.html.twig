{%- if value|length > length -%}
    <span data-toggle="tooltip" data-placement="{{ tooltipPlacement }}" title="{{ value }}">
{%- endif -%}
{{- value|truncate(length, preserve, separator) -}}
{%- if value|length > length -%}
    </span>
{%- endif -%}
